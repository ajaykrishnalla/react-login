import React from "react";

const Header = () => {
  return (
    <nav className="nav nav-bar bg-primary">
      <h1 className="text-center" style={{ color: "white" }}>
        REACT CRUD OPERATIONS USING CONTEXTAPI
      </h1>
    </nav>
  );
};

export default Header;
